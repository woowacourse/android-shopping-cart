<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:shopping="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="java.util.List"/>
        <import type="woowacourse.shopping.data.model.history.RecentProduct"/>

        <variable
            name="recentProducts"
            type="List&lt;RecentProduct>" />

        <variable
            name="adapter"
            type="woowacourse.shopping.presentation.home.HistoryAdapter" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_history_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="20dp"
            android:maxLines="1"
            android:text="@string/header_history"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="최근 본 상품" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_history"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/tv_history_header"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="horizontal"
            android:adapter="@{adapter}"
            shopping:data="@{recentProducts}"
            tools:listitem="@layout/item_product_history" />

        <com.google.android.material.divider.MaterialDivider
            android:layout_width="match_parent"
            android:layout_height="12dp" />

    </LinearLayout>
</layout>
