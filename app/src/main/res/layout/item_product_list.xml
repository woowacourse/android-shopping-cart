<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>

        <variable
            name="productUiModel"
            type="woowacourse.shopping.uimodel.ProductUiModel" />

        <variable
            name="clickListener"
            type="woowacourse.shopping.productlist.ProductListClickAction" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="6dp"
        android:onClickListener="@{()->clickListener.onProductClicked(productUiModel.id)}">

        <ImageView
            android:id="@+id/iv_product_item"
            loadImage="@{productUiModel.imageUrl}"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            tools:src="@drawable/ic_launcher_background"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
        
        <ImageView
            android:id="@+id/button_product_list_add_to_cart"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:background="@drawable/oval_white_plus_button"
            android:onClickListener="@{()->clickListener.onAddButtonClicked(productUiModel.id)}"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="@id/iv_product_item"
            app:layout_constraintBottom_toBottomOf="@id/iv_product_item"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            android:visibility="visible"
            />

        <LinearLayout
            android:id="@+id/button_product_item_quantity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginHorizontal="8dp"
            android:layout_marginBottom="8dp"
            app:layout_constraintStart_toStartOf="@id/iv_product_item"
            app:layout_constraintEnd_toEndOf="@id/iv_product_item"
            app:layout_constraintBottom_toBottomOf="@id/iv_product_item"
            android:background="@drawable/rect_white_round"
            android:visibility="gone"
            >

            <ImageView
                android:id="@+id/button_product_item_minus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/oval_white_minus_button"
                android:onClickListener="@{()->clickListener.onMinusButtonClicked(productUiModel.id, productUiModel.quantity)}"
                app:layout_constraintDimensionRatio="1:1"/>
            
            <TextView
                android:id="@+id/textview_product_quantity"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@{String.valueOf(productUiModel.quantity)}"
                android:textSize="18sp"/>

            <ImageView
                android:id="@+id/button_product_item_plus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/oval_white_plus_button"
                android:onClickListener="@{()->clickListener.onPlusButtonClicked(productUiModel.id, productUiModel.quantity)}"
                app:layout_constraintDimensionRatio="1:1"/>

        </LinearLayout>


        <TextView
            android:id="@+id/tv_product_list_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:fontFamily="@font/roboto_bold"
            android:maxLines="1"
            android:text="@{productUiModel.name}"
            android:textColor="@color/black"
            android:textSize="18sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_product_item"
            tools:text="PET보틀-정사각형이지롱 이건 길고 긴 글자" />

        <TextView
            android:id="@+id/tv_product_list_price"
            formattedPrice="@{productUiModel.price}"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textColor="@color/gray_700"
            android:textSize="16sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_product_list_name"
            app:layout_goneMarginStart="6dp"
            tools:text="10,000원" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
