<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="woowacourse.shopping.view.detail.ProductDetailViewModel" />

        <variable
            name="handler"
            type="woowacourse.shopping.view.detail.ProductDetailEventHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/product_detail"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/iv_product_detail_image"
            productImage="@{viewModel.productItem.product.imageUrl}"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:contentDescription="@string/content_description_product_image"
            android:scaleType="centerCrop"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/sample_product_detail_image" />

        <TextView
            android:id="@+id/tv_product_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{viewModel.productItem.product.name}"
            android:textColor="@color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_product_detail_image"
            tools:text="[든든] 동원 스위트콘" />

        <View
            android:id="@+id/view_divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_centerInParent="true"
            android:layout_marginVertical="10dp"
            android:background="@color/light_gray"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_product_name" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/cl_price_quantity_wrapper"
            android:layout_width="match_parent"
            android:layout_height="42dp"
            android:layout_margin="10dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view_divider">

            <TextView
                android:id="@+id/tv_product_price"
                priceWithFormat="@{viewModel.productItem.product.price * viewModel.productItem.quantity}"
                android:layout_width="wrap_content"
                android:layout_height="0dp"
                android:gravity="center"
                android:textColor="@color/black"
                android:textSize="20sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="99,800원" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="0dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <TextView
                    android:id="@+id/tv_decrease_quantity"
                    android:layout_width="42dp"
                    android:layout_height="match_parent"
                    android:background="@drawable/background_layout_product_counter_decrease"
                    android:clickable="true"
                    android:gravity="center"
                    android:onClick="@{() -> handler.onDecreaseQuantity()}"
                    android:text="@string/button_minus"
                    android:textColor="@color/dark_gray"
                    android:textSize="22sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tv_quantity"
                    android:layout_width="42dp"
                    android:layout_height="match_parent"
                    android:background="@color/white"
                    android:clickable="true"
                    android:gravity="center"
                    android:text="@{String.valueOf(viewModel.productItem.quantity)}"
                    android:textColor="@color/dark_gray"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    tools:text="1" />

                <TextView
                    android:id="@+id/tv_increase_quantity"
                    android:layout_width="42dp"
                    android:layout_height="match_parent"
                    android:background="@drawable/background_layout_product_counter_increase"
                    android:clickable="true"
                    android:gravity="center"
                    android:onClick="@{() -> handler.onIncreaseQuantity()}"
                    android:text="@string/button_plus"
                    android:textColor="@color/dark_gray"
                    android:textSize="22sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>

        <LinearLayout
            isGone="@{viewModel.lastProduct == null}"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:background="@drawable/background_item_shopping_cart"
            android:clickable="true"
            android:onClick="@{() -> handler.onSelectRecentProduct()}"
            android:orientation="vertical"
            android:padding="15dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/cl_price_quantity_wrapper">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/title_most_recent_product"
                android:textColor="@color/teal"
                android:textSize="12sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{viewModel.lastProduct.name}"
                android:textSize="18sp"
                tools:text="PET보틀-정사각형(500ml)" />
        </LinearLayout>

        <Button
            android:id="@+id/btn_add_to_cart"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:backgroundTint="#04C09E"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:onClick="@{() -> handler.onAddToCart()}"
            android:text="@string/button_text_add_to_shopping_cart"
            android:textSize="20sp"
            android:textStyle="bold"
            app:cornerRadius="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
